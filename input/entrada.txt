import numpy
def FuntionUpperCamelCase(X):
    X = X+2
    return X
def funtionLowerCamelCase(X):
    X = X-2
    return X
X_train = FuntionUpperCamelCase(X_train)
MAYUS = 0, minus = 100, i = 1, fila = 2
X_test = X_train -i
dir = X_test
if MAYUS > 0:
    X_test +1
for i in fila:
    for valor in fila:
        valor_i = i+valor
        print(valor_i)
numeros = [18, 6, 4, 15, 55, 78, 12, 9, 8]
contador = 0
for numero in numeros:
    if numero > 10:
        contador = contador +1
print (contador)
while minus < 100:
    X_test -1
    minus-1
    while minus < 100:
        X_test -1
        minus-1
print (contador)